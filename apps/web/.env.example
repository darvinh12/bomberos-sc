# API destino (cuando hay backend levantado)
NEXT_PUBLIC_API_URL=http://localhost:8000
API_INTERNAL_URL=http://localhost:8000

# Modo DEMO temporal: usa fixtures locales en src/lib/demo-fixtures.ts.
# Cuando esté en 1, el frontend funciona SIN backend.
# Cualquier usuario/password permite ingresar.
# Eliminar antes de producción.
NEXT_PUBLIC_DEMO_MODE=0
